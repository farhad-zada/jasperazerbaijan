import React from "react";

const GetStartedButton = ({ className }) => {
  return (
    <button className={`rounded-2xl p-4 w-full ${className}`}>
      Get Started
    </button>
  );
};

export default GetStartedButton;
